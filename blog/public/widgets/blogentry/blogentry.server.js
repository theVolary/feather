jojo.ns("blog");

blog.blogentry = jojo.widget.create({
	name: "blog.blogentry",
	path: "/widgets/blogentry/",
	prototype: {
		initialize: function($super, options) {
			$super(options);
		}
	}		
});
