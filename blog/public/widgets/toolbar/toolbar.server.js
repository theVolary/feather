feather.ns("blog");
blog.toolbar = feather.widget.create({
	name: "blog.toolbar",
	path: "widgets/toolbar/",
	prototype: {
    initialize: function($super, options) {
      $super(options);
    }
  }		
});
