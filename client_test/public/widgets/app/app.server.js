jojo.ns("jojojs.client");

jojojs.client.app = jojo.widget.create({
    name: "jojojs.client.app",
    path: "widgets/app/",
    prototype: {
        initialize: function($super, options) {
            $super(options);
        }
    }       
});
