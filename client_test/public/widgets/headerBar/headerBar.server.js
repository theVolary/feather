jojo.ns("jojojs.client");

jojojs.client.headerBar = jojo.widget.create({
    name: "jojojs.client.headerBar",
    path: "widgets/headerBar/",
    prototype: {
        initialize: function($super, options) {
            $super(options);
        }
    }       
});
