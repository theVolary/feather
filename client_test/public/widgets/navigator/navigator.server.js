jojo.ns("jojojs.client");

jojojs.client.navigator = jojo.widget.create({
    name: "jojojs.client.navigator",
    path: "widgets/navigator/",
    prototype: {
        initialize: function($super, options) {
            $super(options);
        }
    }       
});
