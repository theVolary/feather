jojo.ns("jojojs.client");

jojojs.client.infoPanel = jojo.widget.create({
    name: "jojojs.client.infoPanel",
    path: "widgets/infoPanel/",
    prototype: {
        initialize: function($super, options) {
            $super(options);
        }
    }       
});
