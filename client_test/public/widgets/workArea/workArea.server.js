jojo.ns("jojojs.client");

jojojs.client.workArea = jojo.widget.create({
    name: "jojojs.client.workArea",
    path: "widgets/workArea/",
    prototype: {
        initialize: function($super, options) {
            $super(options);
        }
    }       
});
